[pytest]
DJANGO_SETTINGS_MODULE = rateukma.settings.testing
python_files = test_*.py
addopts = --reuse-db --strict-markers --strict-config
markers =
    e2e: mark test as end-to-end
    integration: mark test as integration
