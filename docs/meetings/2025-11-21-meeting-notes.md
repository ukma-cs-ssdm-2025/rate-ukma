## Action Items

### **Andrii**
- Tasks for partially covered traceability matrix.
- Finish scatterplot.
- E2E tests (Filters).

### **Katia**
- Bug: Course type filter returns nothing when filter is applied.
- Faculties colours under Filters section (#257).

### **Milana**
- Course details page – pin user’s comment (#260).
- Update rating on My Ratings page – show modal instead of redirecting to course details page (#260).
- Pseudo-random test on scraper (Andrii will add a screenshot to the task).

### **Nastia**
- Investigate why images with no name and version are pushed to repo.
- Resolve cancelled concurrent jobs.
- Idea: run Live Deployment if Staging deployment succeeds — setup now, enable after more E2E tests are ready.
- E2E tests (Ratings).
- Check Live load balancing — large database requests for enrollments and course details are slower than expected.

### **Low Priority**
- Bug: save speciality reference in Course Django model.
- Save avg calculations in DB (not dynamically).
- Tests coverage- improve % gradually
