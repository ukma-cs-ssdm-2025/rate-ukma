# Presentations

Marp decks. Source is `<deck>/slides.md`, layout is the shared `theme.css`.

```sh
node docs/presentations/render.mjs fall-2026
```

Writes `slides.html` and `slides.pdf`; both are committed. Works on Windows too.

Conventions: `.agents/skills/slides/SKILL.md`.
